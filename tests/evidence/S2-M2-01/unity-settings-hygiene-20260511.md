# S2-M2-01 Unity Settings Hygiene

**Date:** 2026-05-11
**Status:** PASS
**Scope:** Unity ProjectSettings hygiene after S2-M2-01 closure.

## Changes Verified

- `ProjectSettings/TagManager.asset` was regenerated by Unity `6000.3.14f1`, not hand-edited. The only tracked diff is Unity serialization of empty layer entries from `-` to `- `.
- `ProjectSettings/URPProjectSettings.asset` is Unity-generated Universal Render Pipeline project settings and is now included for reproducible project settings.
- `.gitattributes` adds `ProjectSettings/TagManager.asset whitespace=-blank-at-eol`, scoped only to the Unity-regenerated file. This preserves global trailing-whitespace hygiene while allowing Unity's canonical empty-layer YAML serialization.
- Six untracked iterative raw smoke logs from the S2-M2-01 compatibility loop were deleted as superseded noise. The committed canonical smoke report remains `tests/evidence/S2-M2-01/unity-combat-bridge-smoke-20260510.md`.

## Pre-Fix State

The committed S2-M2-01 compatibility logs recorded the Unity parser warning:

```text
Unable to parse file ProjectSettings/TagManager.asset: [Parser Failure at line 41: Expect ':' between key and value within mapping]
```

The warning appeared in the iterative raw smoke logs at `tests/evidence/S2-M2-01/unity-combat-bridge-smoke-*.log:98-99`, including the committed `tests/evidence/S2-M2-01/unity-combat-bridge-smoke-post-fix-20260511.log:98` through `:99`.

## URP Tracking Decision

`ProjectSettings/URPProjectSettings.asset` contains project-wide Universal Render Pipeline editor settings:

- `Unity.RenderPipelines.Universal.Editor::UnityEditor.Rendering.Universal.UniversalProjectSettings`
- `m_LastMaterialVersion: 10`
- `m_ProjectSettingFolderPath: URPDefaultResources`

It does not contain user-specific paths, Unity Hub install paths, account data, or local machine preferences, so it is tracked as reproducible project configuration.

## Commands

```powershell
Remove-Item -LiteralPath ProjectSettings\TagManager.asset -Force
```

Result: removed the tracked malformed settings file so Unity could recreate it.

```powershell
& "C:\Program Files\Unity\Hub\Editor\6000.3.14f1\Editor\Unity.exe" -batchmode -nographics -projectPath "N:\GravenSpire" -quit -logFile "N:\GravenSpire\tests\evidence\S2-M2-01\unity-settings-regenerate-20260511.tmp.log"
```

Result: Unity recreated `ProjectSettings/TagManager.asset` and exited batchmode successfully. The temporary regeneration log was deleted after the clean verification run below.

```powershell
& "C:\Program Files\Unity\Hub\Editor\6000.3.14f1\Editor\Unity.exe" -batchmode -nographics -projectPath "N:\GravenSpire" -executeMethod Gravenspire.Editor.GravenspireM2CombatBridgeVerificationRunner.Run -logFile "N:\GravenSpire\tests\evidence\S2-M2-01\unity-settings-hygiene-bridge-smoke-20260511.log"
```

Result: PASS. The log opens `_DevEntry.unity` at `tests/evidence/S2-M2-01/unity-settings-hygiene-bridge-smoke-20260511.log:289` and `:291`, then records the M2 bridge evidence write with exit code `0` at `tests/evidence/S2-M2-01/unity-settings-hygiene-bridge-smoke-20260511.log:401`.

```powershell
Select-String -LiteralPath tests\evidence\S2-M2-01\unity-settings-hygiene-bridge-smoke-20260511.log -Pattern 'Unable to parse file ProjectSettings/TagManager.asset','Parser Failure' -SimpleMatch
```

Result: PASS. No matches; the prior `ProjectSettings/TagManager.asset` parser warning is absent from the clean bridge smoke log.

```powershell
git diff --check
```

Result: PASS after adding the per-file `.gitattributes` exception for `ProjectSettings/TagManager.asset`. Other files remain covered by the normal whitespace gate.

## Canonical Smoke Report

The runner rewrote the existing canonical smoke report without content drift:

- `tests/evidence/S2-M2-01/unity-combat-bridge-smoke-20260510.md:7` records `PASS`.
- `tests/evidence/S2-M2-01/unity-combat-bridge-smoke-20260510.md:25` records `PASS no_errors_or_exceptions`.
- `tests/evidence/S2-M2-01/unity-combat-bridge-smoke-20260510.md:46` and `:50` record no captured warnings or errors.

## Blockers

None.
