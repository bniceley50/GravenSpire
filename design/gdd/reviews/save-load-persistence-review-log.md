# Save / Load & Persistence Review Log

## Review — 2026-04-23 — Verdict: APPROVED
Scope signal: L
Specialists: none (lean mode — single-session analysis)
Blocking items: 0 | Recommended: 0 | Nice-to-have: 1
Summary: First-pass approval. All 8 required sections present (plus Locked Inputs, Summary, Visual/Audio, UI Requirements, Open Questions). Every locked upstream contract (save-integrity rule HMAC/version/migration/sanitization; world-structure.md Rule 8 SaveCheckpointing data shape; Rule 12 save_mutex_max_ms attribution; Rule 13 SessionResumeEvent ordering paired with H-CR-13b) is faithfully reproduced without redesign, verified by direct cross-read. Entity-registry sync complete across 4 constants (save_mutex_max_ms, save_autosave_interval, corpse_run_zone_retention_seconds_default, session_catchup_max_real_seconds_default). AC summary-table math verified: 36 criteria = 33 T1-blocking + 3 advisory-at-T1 (H-SL-R10a, H-SL-R10b, H-SL-FM-MigrationStepFailed — promote at first v1→v2 schema bump). Five T1-blocking ADRs correctly scoped as implementation-start gates rather than GDD-approval gates (storage backend, atomicity mechanism, write-failure injection seam, event dispatch semantics, HMAC key-derivation). The Rule-9 ordering narrowing (Save/Load locks only `last_exit_timestamp_utc` delivery pre-`ZoneActiveEvent`; subscriber-state readiness deferred to downstream GDDs) is a deliberate scope boundary that mirrors WS's Rule-13 D3 organic-discovery amendment. The `SaveWriteConfirmed` same-frame dispatch requirement elevated to ADR-tba is the right elevation — preserves WS's `save_mutex_max_ms` deadline-clock semantics that would otherwise silently degrade under a queued-event-bus choice. Nice-to-have (non-blocking, doc hygiene only): the §Dependencies "pre-existing reverse-sync debts" bullet at save-load-persistence.md:296-297 references systems-index.md:172 as stale, but direct read shows that line has already been updated to the ADR-deferred storage-backend wording; the GDD's note about the debt is itself stale.
Prior verdict resolved: First review.
Next steps: `/consistency-check` across the two approved Foundation-layer GDDs (World Structure + Save/Load) to exercise the registry cross-references and bidirectional listings under their intended skill. Downstream GDDs must each declare Save / Load & Persistence in their §Dependencies with matching Hard/Soft classification when authored (per save-load-persistence.md §Dependencies bidirectional-consistency contract). Follow-up batches: (i) promote the WS-local 5-category QA test taxonomy to `.claude/rules/test-standards.md` now that Save/Load has adopted it (completing the WS round-2 deferral condition); (ii) amend DECISIONS.md with any D007+ entries pending from the WS re-review follow-up list; (iii) close the nice-to-have stale self-reference at save-load-persistence.md:296-297. Next Foundation-layer design target: Menus & Settings (systems-index.md row 3, design order #3).
